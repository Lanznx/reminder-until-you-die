# ── Required ──────────────────────────────────────────────────────────────────

# Bot token from Discord Developer Portal
DISCORD_TOKEN=

# Application (Client) ID from Discord Developer Portal
DISCORD_CLIENT_ID=

# PostgreSQL connection string
# e.g. postgres://user:password@host:5432/dbname
DATABASE_URL=

# ── Optional ──────────────────────────────────────────────────────────────────

# How often the ping scheduler ticks (milliseconds, default: 60000 = 1 min)
# PING_CHECK_INTERVAL_MS=60000

# Default ping interval for newly created tasks (minutes, default: 30)
# DEFAULT_INTERVAL_MIN=30

# Number of pings before escalation triggers (default: 5)
# DEFAULT_MAX_PINGS=5

# Health check HTTP port (default: 8080)
# PORT=8080

# ── First-run only ────────────────────────────────────────────────────────────

# Set to "1" once to register slash commands with Discord, then remove.
# REGISTER_COMMANDS=1
